"""MATRICES"""
#CREACION DE MATRICES (por listas de compresion, mas facil)
#matriz = [[0] * columnas for in range(filas)]
#UNA VEZ CREADA, LA LLENAMOS CON DATOS#
#1RO DETECTAMOS FILAS (si no  las tenemos)#
#filas = len(matriz)
#2DO DETECTAMOS COLUMNAS (si no las tenemos)
#columnas = len(matriz[0])
#PARA IMPRIMIR O INGRESAR DATOS DEBEMOS RECORRER TANTO FILAS COMO COLUMNAS (2 for in range)
#for f in range(filas):
#   for c in range(columnas):
#       a = ingreso dato
#       matriz[f][c] = a
#       o
#       print(matriz[f][c])
#   print()
